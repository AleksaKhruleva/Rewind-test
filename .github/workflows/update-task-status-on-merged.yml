name: Update Task Status on Merged

on:
  pull_request:
    types:
      - closed  # Запускается, когда PR закрывается (но проверяем, был ли он слит)

jobs:
  update-status:
    runs-on: ubuntu-latest

    steps:
      - name: Check if PR Was Merged
        run: |
          echo "📌 Target branch: ${{ github.event.pull_request.base.ref }}"
          echo "📝 PR merged? ${{ github.event.pull_request.merged }}"

          if [[ "${{ github.event.pull_request.merged }}" != "true" ]]; then
            echo "❌ PR закрыт, но не был слит. Workflow не выполняется."
            exit 1
          fi
          echo "✅ PR был слит (merged), продолжаем выполнение."

      - name: Extract Issue Key from PR Title
        id: extract_issue_key
        run: |
          echo "📌 Pull request title: ${{ github.event.pull_request.title }}"

          # Проверяем, что заголовок начинается с "REWIND-{число}", где число не начинается с 0
          if [[ "${{ github.event.pull_request.title }}" =~ ^(REWIND-[1-9][0-9]*): ]]; then
            ISSUE_KEY="${BASH_REMATCH[1]}"
            echo "🔑 Найден ключ задачи: $ISSUE_KEY"
            echo "ISSUE_KEY=$ISSUE_KEY" >> $GITHUB_ENV
          else
            echo "❌ Ошибка: Заголовок PR должен начинаться с 'REWIND-{число}:' (где число не начинается с 0)"
            exit 1
          fi

      - name: Update Task Status to "Merged"
        env:
          YANDEX_TRACKER_TOKEN: ${{ secrets.YANDEX_TRACKER_TOKEN }}
          YANDEX_CLOUD_ORG_ID: ${{ secrets.YANDEX_CLOUD_ORG_ID }}
          ISSUE_KEY: ${{ env.ISSUE_KEY }}
          TRANSITION_ID: "merged"
        run: |
          if [ -z "$ISSUE_KEY" ]; then
            echo "❌ Ошибка: ISSUE_KEY не найден!"
            exit 1
          fi

          echo "ℹ️ Обновление задачи $ISSUE_KEY до статуса 'Merged'..."
          
          RESPONSE=$(curl -s -o response.txt -w "%{http_code}" -X POST "https://api.tracker.yandex.net/v2/issues/$ISSUE_KEY/transitions/$TRANSITION_ID/_execute" \
            -H "Authorization: OAuth $YANDEX_TRACKER_TOKEN" \
            -H "X-Cloud-Org-ID: $YANDEX_CLOUD_ORG_ID")

          if [ "$RESPONSE" -ne 200 ]; then
            echo "❌ Ошибка при обновлении задачи. Код ответа: $RESPONSE"
            cat response.txt
            exit 1
          else
            echo "✅ Успешно обновлён статус задачи $ISSUE_KEY до 'Merged'"
          fi
